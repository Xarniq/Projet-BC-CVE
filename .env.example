# ==============================================================================
# Configuration Secu-Audit
# ==============================================================================
# Copiez ce fichier vers .env et remplissez vos valeurs
# cp .env.example .env

# ==============================================================================
# GLPI - Gestion d'inventaire IT
# ==============================================================================
# URL de l'API REST GLPI (sans trailing slash)
GLPI_API_URL=http://votre-serveur-glpi/apirest.php

# Token applicatif GLPI (Administration > Configuration > Générale > API)
GLPI_APP_TOKEN=votre_app_token_ici

# Token utilisateur GLPI (Préférences utilisateur > Clé API distante)
GLPI_USER_TOKEN=votre_user_token_ici

# ==============================================================================
# CVE-Search - Base de données CVE locale
# ==============================================================================
# URL de l'API CVE-Search (Docker)
CTI_API_URL=http://localhost:5000/api

# ==============================================================================
# Paramètres par défaut
# ==============================================================================
# IP cible par défaut pour les scans
IP_TARGET=192.168.1.1
